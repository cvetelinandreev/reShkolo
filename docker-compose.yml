services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: resholo
    ports:
      - "5433:5432"
    volumes:
      - resholo_pgdata:/var/lib/postgresql/data

volumes:
  resholo_pgdata:
