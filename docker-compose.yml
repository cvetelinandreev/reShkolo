services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: reshkolo
    ports:
      - "5433:5432"
    volumes:
      - reshkolo_pgdata:/var/lib/postgresql/data

volumes:
  reshkolo_pgdata:
